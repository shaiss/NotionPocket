{
  "name": "notionpocket",
  "version": "0.0.1",
  "description": " a system to query the Pocket APIs, pull the latest saves, and then add them to a notion database item using the Notion.so APIs",
  "main": "server.js",
  "scripts": {
    "test": "jest --watchAll",
    "start": "node server.js",
    "dumplog": "jest --log-level=debug"
  },
  "author": "Shai Perednik",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "jest": "^29.3.1",
    "supertest": "^6.3.3"
  },
  "directories": {
    "test": "test"
  }
}
